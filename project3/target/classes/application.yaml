spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobportaldb
    username: naveen
    password: StrongPassword123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

spring.thymeleaf:
  cache: false

server:
  port: 8081